# Raw Snowflake pulls — kept local; the published HTML is built from them.
# NOTE: the dashboard HTML is NOT aggregate-only. Since the 8/10 drill-down it embeds
# customer account names for the case-detail modals. Brian was shown that exposure and
# chose to publish as-is; this file is ignored to keep the raw JSON out of the repo, not
# because the HTML is anonymous.
data/
